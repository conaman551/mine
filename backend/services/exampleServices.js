

// If not wrong, then its right
// have final catch return statement as correct, use if cases etc to catch wrong form

function exampleFunction (format) {
    if(format != "x@x.x"){
        return (False, "Error message")
    }
    return (True, "Correct")
}